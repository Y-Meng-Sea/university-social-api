spring.application.name=university-social

server.port=8081

#database configuration
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

#jpa configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT secret key (generate a base64 encoded 256-bit key)
# You can generate one using: openssl rand -base64 32
security.jwt.secret-key=${JWT_SECRET_KEY:ibQ5zkuA4uv9aBIK+jh+igZs+TVq9vDNMobFuKbHf9k=}
security.jwt.expiration-time=3600000

# Mail properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mrmengsea@gmail.com
spring.mail.password=sxokfugkuyvollwr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.config.import=file:.env[.properties]


# google cloud storage
gcp.project-id=${GCP_PROJECT_ID}
gcp.bucket=${GCP_BUCKET}
gcp.credentials.location=${GCP_CREDENTAILS_LOCATION}

#set max and min size to file upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB